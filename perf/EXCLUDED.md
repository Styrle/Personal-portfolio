# Pages excluded from the performance metric

These are orphaned 2021-era files. Nothing on the site links to them, and they
are not part of the measured set because their scores are meaningless noise
that distorts the site average.

| Page | Bytes | Why excluded |
|---|---|---|
| `portfolio/rpcs3.html` | 0 | Empty file. Scored 0 and dragged the average down ~5 points. |
| `portfolio/strength-analysed.html` | 1,441 | Stub with broken relative links (`images/…` from inside `portfolio/`). Scored a meaningless 100. |
| `portfolio/bin-sensor.html` | 1,989 | Stub, same broken links. Scored a meaningless 100. |

They should be deleted or finished — that is a content decision, not a
performance one, so the loop flagged them rather than acting.
